# Core (required)
Pillow>=10.0

# Optional but recommended
pillow-heif>=0.13    # view iPhone / HEIC photos
rawpy>=0.19          # view camera RAW photos (Sony .ARW, Canon .CR2/CR3, Nikon .NEF, etc.)
send2trash>=1.8      # delete to the Recycle Bin (recoverable)
pywebview>=4.4       # open in a native app window instead of a browser

# Only needed to BUILD the standalone .exe (see build_windows.bat)
# pyinstaller>=6.0
